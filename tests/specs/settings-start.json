[
  {
    "name": "Settings shows current configuration",
    "steps": [
      {
        "send": { "callback": "settings:start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "⚙️ Settings\n\nQuiet hours: Not set\nCooldown: 60 min"
            }
          }
        ]
      }
    ]
  }
]
