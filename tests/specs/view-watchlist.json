[
  {
    "name": "View List shows empty watchlist",
    "steps": [
      {
        "send": { "callback": "view_watchlist" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Your watchlist is empty — tap ➕ Add Coin to start tracking."
            }
          }
        ]
      }
    ]
  }
]
